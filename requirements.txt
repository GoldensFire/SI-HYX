PyQt6
qtawesome
yt-dlp
Pillow
pillow-heif
certifi
requests

# Опционально — для экспериментальной вкладки «SiQuester» (по умолчанию выключена).
# Дают настоящие звуковые волны и точный LUFS у аудио/видео. Без них вкладка всё
# равно работает: звук играет, волна рисуется приближённо, LUFS считается через
# bundled ffmpeg. lxml ускоряет разбор .siq (иначе используется xml.etree).
# pyloudnorm — точный LUFS (ITU-R BS.1770-4) поверх soundfile; без него используется
# приближённый пересчёт через тот же soundfile/wave/ffmpeg.
soundfile
pyloudnorm
numpy
lxml

# Экспериментальная вкладка «Поиск пакетов» (sigstats, по умолчанию выключена).
# Без них вкладка покажет сообщение об ошибке импорта, остальное приложение не
# затронуто. mutagen — фолбэк для длительности аудио/видео, если ffprobe не дал
# ответ (см. sigstats/siq.py _probe_duration).
pandas
beautifulsoup4
emoji
mutagen

# Подвкладка «Фото → Удаление объектов/фона» (LaMa, RMBG-2.0). Без них приложение
# работает, но подвкладка покажет заглушку.
# ускорение на видеокарте (удаление объектов и фона идёт в РАЗЫ быстрее):
#   • NVIDIA:                    pip uninstall onnxruntime && pip install onnxruntime-gpu
#   • ЛЮБАЯ видеокарта (Windows): pip uninstall onnxruntime && pip install onnxruntime-directml
# Код сам подхватит GPU (CUDA → DirectML → CPU); ставить можно ТОЛЬКО ОДИН пакет
# onnxruntime* (они конфликтуют). Без GPU-пакета считается на CPU.
opencv-python
onnxruntime
