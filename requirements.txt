# Зависимости SI-HYX. Версии закреплены точно: релиз собирается PyInstaller-ом,
# и «поехавшая» минорная версия ломает сборку раньше, чем это заметно на глаз.
# Обновлять осознанно, по одному пакету, с прогоном тестов
# (см. .github/workflows/tests.yml).

# --- Ядро: GUI, значки, изображения, сеть ---
PyQt6==6.10.1
qtawesome==1.4.2
Pillow==12.2.0
pillow-heif==1.3.0
requests==2.33.1
certifi==2025.11.12          # корневые сертификаты; освежать при каждом релизе

# --- Загрузчик ---
# Единственный пакет БЕЗ пина: yt-dlp чинит поломки YouTube/TikTok/… релизами
# раз в несколько дней, закрепить версию = получить нерабочую загрузку.
yt-dlp

# --- Вкладка «SiQuester» (по умолчанию выключена) ---
# Дают настоящие звуковые волны и точный LUFS (ITU-R BS.1770-4). Без них вкладка
# всё равно работает: звук играет, волна рисуется приближённо, LUFS считается
# через bundled ffmpeg, .siq разбирается через xml.etree вместо lxml.
soundfile==0.13.1
pyloudnorm==0.2.0
numpy==2.3.5
lxml==6.1.1

# --- Вкладка «Поиск пакетов» (sigstats, по умолчанию выключена) ---
# Без них вкладка покажет ошибку импорта, остальное приложение не затронуто.
pandas==3.0.2
beautifulsoup4==4.14.3
emoji==2.15.0
mutagen==1.47.0              # длительность медиа, когда ffprobe не дал ответ

# --- Вкладка «Фото» → удаление объектов/фона (LaMa, RMBG-2.0) ---
# Без них приложение работает, но подвкладка покажет заглушку.
opencv-python==4.13.0.92
onnxruntime==1.27.0
# Ускорение на видеокарте (в РАЗЫ быстрее). Ставить можно ТОЛЬКО ОДИН пакет
# onnxruntime* — они конфликтуют; код сам выберет CUDA → DirectML → CPU:
#   NVIDIA:           pip uninstall onnxruntime && pip install onnxruntime-gpu
#   любая (Windows):  pip uninstall onnxruntime && pip install onnxruntime-directml
