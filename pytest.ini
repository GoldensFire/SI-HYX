[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: интеграционные тесты (несколько модулей / файловая система / zip)
    qt: тесты, которым нужен QApplication
filterwarnings =
    ignore::DeprecationWarning
