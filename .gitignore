# Сборка PyInstaller
build/
dist/
*.spec

# Python
__pycache__/
*.pyc

# Внешние бинарники (большие, кладутся в релиз-архив, не в репозиторий)
bin/
# Модель LaMa (~200 МБ) — тоже в релиз-архив, а не в git
models/

# Локальные/служебные файлы
settings.json
*.log
*_original_backup.py
# Случайный лог-маркер сборки build.bat (echo «Version: … -> …» в файл)
SI-HYX v*
YouTubecookies.txt
*cookies*.txt

graphify-out/
GRAPH_REPORT.md